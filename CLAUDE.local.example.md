# 本地 Claude Code 偏好示例

> 将本文件复制为项目根目录的 `CLAUDE.local.md` 后按需填写。`CLAUDE.local.md` 是本机私有指令，**不得提交**；复制前先确认自己的 Git ignore 配置会保护它。

## 可填写内容

- 本机可用的非敏感工具或数据目录（不要写入 Token、Cookie、账号或绝对个人路径）。
- 仅适用于个人工作流的沟通偏好、输出粒度和本地验证命令。
- 需要人工确认的本地操作边界。

## 不应填写

- API 密钥、Token、Cookie、登录态、私人账本或持仓明细。
- 会预设某个公司看多/看空的投资立场。
- 应适用于所有贡献者的项目规则；这类规则应放入 `CLAUDE.md` 或 `.claude/rules/`。
